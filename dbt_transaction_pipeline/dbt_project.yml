name: 'dbt_transaction_pipeline'
version: '1.0.0'
config-version: 2

profile: 'dbt_transaction_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_transaction_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
